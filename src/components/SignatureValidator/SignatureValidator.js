import React from 'react';

const SignatureValidator = () => {
    return (
        <div>
            <h4>Signature Validator Page is not available</h4>
        </div>
    );
};

export default SignatureValidator;